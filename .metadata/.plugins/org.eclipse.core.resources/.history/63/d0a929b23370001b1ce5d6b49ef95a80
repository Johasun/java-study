import java.util.ArrayList;
import java.util.HashMap;

public class Bank {
	HashMap<String, String> accounts; //계좌
	ArrayList<Account> accountinfo; // 계좌정보
	int totalAccount; //총계좌수
	
	//계좌를 생성한다
	void Bank(String accountNo, String name) {
		
		accounts = new HashMap<String, String>();
		accounts.put(accountNo, name);
		
		accountinfo = new ArrayList<Account>();
		accountinfo.add(new Account(accountNo, name));
		
		totalAccount++;
	}
	
	//계좌를 찾는다(계좌번호로)
	Account getAccount(String accountNo) {
		//1. HashMap에 키값에 accountNo을 넣어서 있는지 확인
		accountinfo.get
		for(int i = 0; i<accountinfo.size(); i++) {
			
		}
		return;
	}
	
	//계좌를 찾는다(소유자명으로)
	Account[] findAccounts(String name) {
		return;
	}
	
	//계좌목록을 본다
	void Accounts() {
		System.out.println("= 전체 계좌 목록 =");
		for(Account a : accountinfo) {
			System.out.println(a);
		}
	}
	
	//총계좌수를 반환한다
	void TotalAccount() {
		System.out.println("총 계좌 수 : "+totalAccount);
	}
}
